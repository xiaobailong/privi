# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/
Thumbs.db
*.tmp
*.bak

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code (keep shared settings, ignore user/workspace state)
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json

# Flutter/Dart/Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
coverage/
# NOTE: pubspec.lock IS committed (this is an app, not a package) for reproducible builds.

# 本地构建产物：APK/AAB 通过 GitHub Release 分发（build.bat 第 7 步自动发布），不入库
/privi-*.apk
/privi-*.apk.sha256

# Cline 工作临时目录：整目录忽略（约定见 .clinerules/tmp-files.md），可随时整目录删除
/tmp/

# Generated code (regenerated by build_runner)
*.g.dart
*.freezed.dart
*.config.dart
*.mocks.dart

# FVM (Flutter Version Management) — keep .fvmrc, ignore the local SDK symlink/cache
.fvm/flutter_sdk
.fvm/versions
.fvm/flutter_config.json

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android (project root is Android root)
gradle-wrapper.jar
/.gradle
/.gradle_home
/.BUILD_NUM
/android/.gradle
/captures/
/gradlew
/gradlew.bat
**/local.properties
**/GeneratedPluginRegistrant.java
key.properties
*.jks
*.keystore
.cxx/

# iOS / macOS (not a target, but ignore if generated)
**/ios/Pods/
**/ios/.symlinks/
**/ios/Flutter/Flutter.framework
**/ios/Flutter/Flutter.podspec
**/macos/Flutter/ephemeral/
**/linux/flutter/ephemeral/
**/windows/flutter/ephemeral/

# Local env / secrets / machine-specific
.env
.env.*
!.env.example
*.local
secrets/
*.pem
!shorebird_public.pem
*.p12
credentials.json
service-account*.json

# Internal design / architecture docs (local only — not published)
docs/

# Editor / OS junk
*.orig
*~
.fleet/
.history/