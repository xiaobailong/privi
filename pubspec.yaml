name: privi
description: >-
  Privi — a personal, on-device Android media vault by kcng0. Hides photos and
  videos from the system gallery, supports 1–3 heart ratings, favorites,
  playlist playback with built-in ExoPlayer, and pattern/PIN + biometric lock.
  Dark theme only. MIT license.
publish_to: "none" # personal app — never published to pub.dev
version: 1.0.25+30

# NOTE: version constraints below are sensible starting points. Run
#   flutter pub get   (then optionally `flutter pub upgrade --major-versions`)
# to resolve and lock exact versions; confirm on pub.dev. See DEVELOPMENT.md.
environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State management (manual Riverpod 3.x providers) — see docs/03-architecture/state-management.md
  flutter_riverpod: ^3.0.0

  # Local database (metadata) — see docs/03-architecture/data-model.md
  drift: ^2.20.0
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.24

  # Storage & paths
  path_provider: ^2.1.4
  path: ^1.9.0
  shared_preferences: ^2.3.2
  package_info_plus: ^9.0.1
  http: ^1.6.0
  pub_semver: ^2.2.0
  shorebird_code_push: ^2.0.7
  restart_app: ^1.8.3
  uuid: ^4.5.0

  # Import: file picker + share intent — see docs/02-design/screens/06-import.md
  file_picker: ^10.0.0
  receive_sharing_intent: 1.9.0
  # System gallery albums for Visible tab
  photo_manager: ^3.6.0
  permission_handler: ^11.3.1

  # Playback: native ExoPlayer via MethodChannel + ActivityResult VLC hand-off
  android_intent_plus: ^5.2.0

  # Security: PIN hash + biometric — see docs/03-architecture/security.md
  local_auth: ^2.3.0
  local_auth_android: any
  flutter_secure_storage: ^9.2.2
  crypto: ^3.0.5
  cryptography: ^2.9.0

  # Formatting
  intl: ^0.20.2

  # Share selected gallery items (Visible → More)
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  xml: ^6.6.1

  flutter_lints: ^5.0.0

  # Code generation
  build_runner: ^2.4.13
  drift_dev: ^2.20.3

  # Riverpod lint rules for the manual providers (run via custom_lint).
  custom_lint: ^0.8.1
  riverpod_lint: ^3.0.0

  # Host-side Drift tests (open.overrideFor libsqlite3.so.0 on Linux/WSL).
  sqlite3: ^2.9.0

flutter:
  generate: true
  uses-material-design: true
  assets:
    - assets/branding/app_icon.png
    - assets/branding/app_icon_192.png
    - shorebird.yaml